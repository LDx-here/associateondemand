/**
 * @deprecated Re-exports from `./airtable/fields`. New code should import
 * from `@/lib/airtable/fields` (TABLES + SPEC_FIELDS) per BUILD_SPEC §3
 * layout. `LEGACY_FIELDS` was retired 2026-05-26 after the live base was
 * renamed to snake_case.
 */

export * from "./airtable/fields";
