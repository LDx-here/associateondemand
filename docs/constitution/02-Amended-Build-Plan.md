# Amended AssociateOnDemand Build Plan (pointer)

This repository follows an **eight-phase** build plan maintained in Cursor (AssociateOnDemand: From Prototype to Functional Multi-Agent Firm).

- **Do not treat this file as the source of truth for phase details.** The canonical plan is the version you approved in Cursor (including phase resequencing, Presidio tier gates, constitution files, Research SKILL loading rules, etc.).
- **After major plan edits**, paste or attach an export into `/docs/` (for example `/docs/amended-plan-snapshot.md`) so agents and collaborators can diff against paper without Cursor.

**Phase summary**

0. True foundation — Next.js + FastAPI + Docker + governance + constitution pack  
1. Airtable + read-only dashboard  
2. CRUD + case assessment + unified timeline + search-only Command Panel  
3. Strong Reader (Presidio-gated) + Obsidian ingest  
4. PM orchestrator + Five-Anchors + training loop + Research agent  
5. Pattern recognition + strategy + knowledge map  
6. Tiered eImmigration  
7. Production hardening  

See also [constitution README](README.md).
