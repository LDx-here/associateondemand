# PENDING — Research Memo SKILL.md

**Status:** Placeholder. Phase 4’s Research Agent will **refuse to run** until this file is replaced with your full multi-source research protocol (Midpage Fastcase hierarchy, country-conditions ranking, multi-year retrieval, source-documentation table rules).

**Where to deposit the real SKILL**

1. Paste your SKILL content into `AssociateOnDemand/.incoming/` on disk (see `.incoming/.gitkeep` for filenames).
2. Copy it here as `docs/constitution/04-Research-Memo-SKILL.md` (overwrite this stub).

Alternatively, rename your existing SKILL file when you paste it:

```bash
cp ".incoming/Research-Memo-SKILL.md" "docs/constitution/04-Research-Memo-SKILL.md"
```

Do **not** hand-retype protocol rules in Python; load this document at agent startup.
