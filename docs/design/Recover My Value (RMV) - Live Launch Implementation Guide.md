# Recover My Value (RMV) - Live Launch Implementation Guide

**Author:** Manus AI
**Date:** April 28, 2026
**Project:** Recover My Value (RMV) / Lightship

## 1. Executive Summary

This guide provides a comprehensive package to accelerate the development of your Recover My Value (RMV) application, moving beyond prototypes to a functional, data-driven system. The focus is on enabling you to quickly achieve a high-fidelity UI that mirrors leading legal tech platforms, seamlessly integrate your existing case data, and establish an efficient workflow for debugging and refining the code generated by Cursor. This package is designed to get you a "usable site tonight" by leveraging Cursor for rapid implementation of key components.

## 2. Key Components of the Live Launch Package

This package comprises three critical elements:

1.  **UI Cloning & Component Code (Legora/Clio Aesthetic)**: Provides the specific design system configurations and component structures to ensure your RMV application has a professional, polished look and feel, consistent with top-tier legal SaaS platforms.
2.  **Data Normalization Adapter Design (E-immigration/Clio)**: Outlines the architecture for ingesting and transforming your existing case data (from E-immigration or Clio CSV/JSON exports) into RMV's internal data model, allowing you to populate your dashboard with real information.
3.  **Code Review & Fix Workflow**: Establishes a clear, repeatable process for collaborating with Cursor to identify, diagnose, and resolve code-related issues, ensuring the stability and correctness of your application.

## 3. Implementation Strategy

The strategy for this phase is to use Cursor as your primary development partner, feeding it these detailed specifications to generate and refine the necessary code. This approach minimizes manual coding and maximizes the efficiency of your build process.

### 3.1. Step 1: Provide All Documents to Cursor

Ensure all the following `.md` files are placed in the project directory that Cursor is monitoring. This provides Cursor with the complete context needed to understand your requirements and generate accurate code.

*   `/home/ubuntu/RMV_UI_Cloning_Code.md`
*   `/home/ubuntu/RMV_Data_Normalization_Adapter_Design.md`
*   `/home/ubuntu/RMV_Code_Review_Fix_Workflow.md`
*   *(Also ensure all previously generated documents like PRD, SRD, Architecture, etc., are still accessible to Cursor)*

### 3.2. Step 2: Implement UI Cloning and Component Building

**Cursor Prompt:**

```markdown
```markdown
**Goal:** Implement the "Legora/Clio" aesthetic for the RMV application.

**Instructions:**
1.  Read the `RMV_UI_Cloning_Code.md` file.
2.  Update the `tailwind.config.js` file in the project root with the provided color palette, typography, and shadow definitions.
3.  Refactor existing UI components (or build new ones if they don't exist) strictly adhering to the "Key Component Structures" outlined in `RMV_UI_Cloning_Code.md`. Focus on the "Active Matters" Data Table, "Dashboard KPI" Card, and "Matter Details" layout.
4.  Ensure all new and existing components use the defined Tailwind classes and design principles to perfectly replicate the high-end, clean aesthetic of Legora/Clio.
5.  Prioritize the `web` frontend for this initial implementation, but ensure the styling is adaptable for `mobile` (Expo) components as well.
```

### 3.3. Step 3: Implement Data Normalization Adapter

Once the UI has the desired look, the next step is to make it functional with your real data. This involves building the data normalization adapter.

**Cursor Prompt:**

```markdown
**Goal:** Build the E-immigration/Clio Data Normalization Adapter to import case data into RMV.

**Instructions:**
1.  Read the `RMV_Data_Normalization_Adapter_Design.md` file.
2.  Create a new FastAPI endpoint (e.g., `POST /api/import/eimmigration`) that accepts a CSV or JSON file upload.
3.  Implement the data transformation logic as described in the document, mapping the external data fields to RMV's internal data model (Matter, Client, Task, Deadline, Document).
4.  Ensure robust data validation and error handling for imported data.
5.  Develop a simple UI component (e.g., a file upload form) in the RMV dashboard that allows users to upload their E-immigration/Clio export files and trigger this API endpoint.
6.  Provide clear feedback to the user on the import status (success, errors, number of records imported).
7.  Generate a sample CSV template file that aligns with the expected input format for E-immigration data, as described in the design document.
```

### 3.4. Step 4: Establish Code Review & Fix Workflow

As Cursor generates code, you will inevitably encounter issues. This workflow ensures you can effectively collaborate with Cursor to debug and refine the application.

**Cursor Prompt (for debugging):**

```markdown
**Goal:** Help me debug and fix a specific issue in the RMV application.

**Instructions:**
1.  I will provide you with the following:
    *   A clear description of the problem.
    *   Any relevant error messages or console logs.
    *   The code snippet(s) I suspect are causing the issue (if known).
    *   Any additional context that might be helpful.
2.  Analyze the provided information and propose a solution. This might include:
    *   Direct code modifications.
    *   Instructions for dependency updates.
    *   Configuration changes.
    *   Further debugging steps.
3.  Ensure your proposed solution aligns with the overall RMV architecture and design principles.

**Example Usage (You would replace this with your actual problem):**

**Problem:** The RMV dashboard is showing a blank screen instead of the "Active Matters" table.

**Error Messages/Logs:**
```
TypeError: Cannot read properties of undefined (reading 'map')
    at ActiveMattersTable (webpack-internal:///./src/components/ActiveMattersTable.tsx:25:20)
    at renderWithHooks (webpack-internal:///./node_modules/react-dom/cjs/react-dom.development.js:16300:18)
    ...
```

**Relevant Code:**

**File: `src/components/ActiveMattersTable.tsx`**
```typescript
// ... (lines 1-20)

const ActiveMattersTable: React.FC<{ matters: Matter[] }> = ({ matters }) => {
  return (
    <div className="overflow-x-auto">
      <table className="min-w-full divide-y divide-surface-border">
        <thead>
          <tr>
            {/* ... table headers */}
          </tr>
        </thead>
        <tbody className="bg-surface divide-y divide-surface-border">
          {matters.map((matter) => (
            <tr key={matter.id}>
              {/* ... matter data */}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
};

export default ActiveMattersTable;
```

**Additional Context:**
"I'm passing `matters` as a prop to `ActiveMattersTable` from `src/app/dashboard/page.tsx`. I suspect `matters` might be undefined when the component first renders."

**Goal:**
"Fix this `TypeError` so the `ActiveMattersTable` component renders correctly, even if the `matters` prop is initially undefined or empty. Ensure a loading state or an empty state message is displayed if no data is available."
```

## 4. Conclusion

This "Live Launch" Implementation Guide, combined with the detailed design documents, provides a robust framework for building a functional and aesthetically pleasing RMV application. By leveraging Cursor with these precise instructions, you can rapidly iterate and bring your vision to life. Remember to continuously feed Cursor with the latest context and use the debugging workflow to address any challenges that arise.
